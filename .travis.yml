language: node_js
node_js:
  - '12'

cache:
  directories:
    - node_modules

script:
  - npm run deploy

deploy:
  provider: surge
  project: ./dist/
  domain: react-typescript-styled-system-boilerplate.surge.sh
  on:
    branch: master